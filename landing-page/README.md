# Orville static landing page

The page explains a recorded support handoff using fictional customer input and real app records. It does not run the Python agent.

Serve only `dist/` as the public root. No build step or application dependencies are required. Fonts are loaded from Google Fonts; font sources and licenses are documented in ASSETS.md.

## Review path

Open the page, follow “Explore the recorded handoff”, and inspect the HARBOR-EXPORT-02 receipt. The expected IDs are GitHub comment 5656400715, Trello card 6aa71b13df248d71096f78bc, and Discord message 1548813613349871726. The displayed status is historical, not a fresh verification. The first app writes occurred before the recorded replays.

Both primary links target the receipt. FAQ disclosure controls work with a keyboard. The page includes reduced-motion handling and noindex metadata. Update both primary actions only after a real accessible demo link is verified.

## Publication boundary

Prepared for Sites static hosting. `.openai/hosting.json` identifies the reserved, unpublished Site. Publish only `dist/`. The parent repository, local state, private production notes, recordings, frame pack and credentials must never be included in the public artifact. Public visitor access requires the owner's publication approval.
